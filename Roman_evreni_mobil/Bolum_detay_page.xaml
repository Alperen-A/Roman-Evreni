<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Roman_evreni_mobil.Bolum_detay_page"
             BackgroundColor="#0a0a0a">

    <Grid x:Name="Ana_kutu" Opacity="0" RowDefinitions="Auto, Auto, *">
        <Image Grid.RowSpan="3" Source="bolum_bg.jpg" Aspect="AspectFill" Opacity="0.4" />
        <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" Padding="20,40,20,20">
            <Label Text="← Geri" TextColor="#f5f5f5" FontSize="16" VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="On_geri_clicked" />
                </Label.GestureRecognizers>
            </Label>
            <Label x:Name="Bolum_baslik_label" Grid.Column="1" Text="" FontSize="20" FontAttributes="Bold" TextColor="#f5f5f5" HorizontalOptions="Center" VerticalOptions="Center" FontFamily="Serif" />
            <Label Grid.Column="2" Text="Kaydet" TextColor="#f5f5f5" FontSize="16" FontAttributes="Bold" VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="On_kaydet_clicked" />
                </Label.GestureRecognizers>
            </Label>
        </Grid>

        <!-- Kelime sayacı -->
        <Label x:Name="Kelime_sayaci" Grid.Row="1" TextColor="#525252" FontSize="12" Padding="20,0,20,10" />

        <!-- Yazı editörü -->
        <ScrollView Grid.Row="2">
            <Editor x:Name="Editor_icerik"
                    Placeholder="Yazmaya başla..."
                    PlaceholderColor="#525252"
                    TextColor="#f5f5f5"
                    BackgroundColor="Transparent"
                    FontSize="16"
                    FontFamily="Serif"
                    AutoSize="TextChanges"
                    Margin="20,0,20,40"
                    TextChanged="On_metin_degisti" />
        </ScrollView>
    </Grid>
</ContentPage>